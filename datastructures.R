# vector
v <- c(1, 2, 3, 4, 5)

# list
l <- list(1, "a", TRUE)

# data frame
df <- data.frame(name = c("Lokesh","Vikki","Vallal","Hamdhan","Gova"), age = c(21,22,23,20,21))

# matrix
m <- matrix(c(1, 2, 3, 4, 5, 6), nrow = 2, ncol = 3)

# array
a <- array(c(1, 2, 3, 4, 5, 6), dim = c(2, 3, 1))

# factor
f <- factor(c("low", "high", "medium", "low", "high","high","high","low"))

print(v)
print(l)
print(df)
print(m)
print(a)
print(f)